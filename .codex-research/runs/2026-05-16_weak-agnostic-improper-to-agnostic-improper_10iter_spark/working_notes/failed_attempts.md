# Failed Attempts

Iteration 10 adds these no-go entries:

- Attempted to continue the Route-D compatibility audit for KMV08/Feldman/potential boosters in `idea_01`.
  - Failure: still no theorem-indexed completion with exact oracle contracts, output assumptions, theorem IDs, and complexity mapping for atlas `err ≤ OPT+β` model.

- Attempted to advance a conditional Route-A wrapper argument from iter-9 invariants.
  - Failure: no proof of simultaneous polynomial mass and adaptive, high-confidence `γ_t` certification for wrapper rounds.

- Attempted to convert the same route into an operational complexity claim with explicit 
  
  `(1/(1/2-β), 1/ε, log(1/δ), n)` dependencies.
  - Failure: missing theorem precision and stopping-time estimator control.

- Attempted to recover an explicit negative theorem in one iteration sweep via explicit `(C,D,F)` templates.
  - Failure: no quantified witness in-file for either `OPT` inflation to near `1/2` or acceptance-collapse at a formal rate.

General summary status after iteration 10: unresolved but useful progress is that all speculative routes were kept conditional, and no claim was promoted beyond the proven equivalence bridge above.
