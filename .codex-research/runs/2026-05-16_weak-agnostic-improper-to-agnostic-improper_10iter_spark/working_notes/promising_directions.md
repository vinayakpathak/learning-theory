# Promising Directions

## Iteration-10 update

- **Primary route:** complete Route D as a theorem-indexed compatibility table for KMV08/Feldman/potential boosters under exact atlas oracle contract.
- **Conditional positive route:** once Route D clears, prove a single wrapper family (likely clipping residual-type) that gives polynomial mass and high-confidence `γ_t` certificates with explicit dependence on `(β, ε, δ, n)`.
- **Fallback negative route:** if Route A cannot be certified, prioritize a quantified `(C,D,F)` obstruction with explicit growth/decay rates (`OPT` inflation-to-`1/2` or exponential mass collapse).
- **Precision branch:** if oracle mismatch or `β→1/2` prevents Route A, pivot to fixed-slack-SQ precision lower-bound theorems that match `{0,1}` output semantics.

## Route discipline (non-theorem policy)
- Keep all positive claims conditional on completed interface mapping and estimator validity.
- Keep all negative claims as **conditional/open** unless complete quantifiers `(β,ε,δ,n)` and construction details are present.
